import React from 'react';

function Header(props) {
    return (
        <div>
            This header will eventually hold nav to achieve stretch goals and also navigate back to home page by clicking the logo
        </div>
    );
}

export default Header;